# Copy this file to .env and adjust values.
# NOTE: .env is ignored by git; .env.example is committed.

# --- App ---
NEXT_PUBLIC_APP_NAME=统一身份认证平台
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Server-side base URL override (optional)
APP_URL=http://localhost:3000

# --- JWT ---
# Required: change to a long random string in production
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING
JWT_EXPIRES_IN=7d

# --- Database (Prisma) ---
# Recommended: set DATABASE_URL explicitly.
# SQLite (default)
DATABASE_URL="file:./data/sso.db"

# Runtime fallback (server-only): if DATABASE_URL is not set, the app will infer it from DB_TYPE + *_ vars.
# Prisma CLI still expects DATABASE_URL.

# DB_TYPE=sqlite
# SQLITE_PATH=./data/sso.db

# DB_TYPE=postgresql
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=sso

# DB_TYPE=mysql  # or mariadb
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=
# MYSQL_DATABASE=sso

# When switching databases, generate Prisma Client using the matching schema file:
# - SQLite:      npm run prisma:generate:sqlite && npm run prisma:dbpush:sqlite
# - PostgreSQL:  npm run prisma:generate:postgresql && npm run prisma:dbpush:postgresql
# - MySQL/Maria: npm run prisma:generate:mysql && npm run prisma:dbpush:mysql
